from skimage import morphology
from skimage.morphology import selem
from skimage.morphology import binary

def create_labels_pixel(sData, pixlIdx, pad):
	# find valid pixels based on truth data and pad region
	# unimplemented
	return sData;








